#!/usr/bin/env node
'use strict';

/**
 * RemotEE Tests
 * @use run suite of tests simulating cli arguments and fake config file
 */

var should = require('should'),
    assert = require('assert'),
    remotee = require('../bin/remotee-sync.js');

require('mocha');

describe('remotee-sync', function(){

});
